#include <cstdint>
#include <string>
#include <vulkan/vulkan.h>

namespace constants
{
	inline const std::string displayname = "Dreams";
	inline const std::string name = "dreams";

	inline const uint32_t version = 1;
}